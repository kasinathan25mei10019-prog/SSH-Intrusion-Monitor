def monitoring(log_file):
    file = open(log_file)
    print("Starting the analyser.......")
    while True :
        line = file.readline()
        if not line:
            time.sleep(1)
            continue
        log_details = line_re(line)
        if log_details:
            activity(log_details)
