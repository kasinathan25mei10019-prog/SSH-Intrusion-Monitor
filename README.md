# SSH-Intrusion-Monitor
The objective of this Python script is to monitor Linux SSH authentication logs (/var/log/auth.log or /var/log/secure) in order to quickly recognize login attempts. It is a lightweight, real-time log analysis tool.
